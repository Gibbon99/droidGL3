#include "gam_droidNetwork.h"

// For each connected client
// Get their current deck
// If there are droids alive on that desk
// For each droid alive - send position to that client - one packet per droid
